name: my first workflow

on: push
jobs:
    say-hello:
        runs-on: ubuntu-latest

        steps:
            - name: Print message
              run: echo "Hello, github actions"
            - name: print name
              run: echo "parv"

